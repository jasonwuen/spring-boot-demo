<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
        http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <dubbo:application name="${application.name}" owner="programmer" organization="dubbox" logger="slf4j"/>

    <dubbo:registry protocol="zookeeper" address="${application.zkHosts}" file="false" client="curator" group="dev"/>

    <dubbo:provider timeout="30000" retries="0" delay="-1" validation="true" loadbalance="random" accesslog="true"/>

    <dubbo:protocol name="dubbo" port="20881" accesslog="true" serialization="kryo"/>

    <dubbo:protocol name="rest" port="${application.server.port}" threads="${application.server.threads}" contextpath="api" server="tomcat"
                    accepts="${application.server.threads}"
                    extension="cn.net.wujun.common.ext.rest.ValidationExceptionMapper,
                    com.alibaba.fastjson.support.jaxrs.FastJsonProvider,
                    com.alibaba.dubbo.rpc.protocol.rest.support.LoggingFilter"/>

    <dubbo:annotation package="${dubbo.annotation.package}"/>

    <!--<context:component-scan base-package="cn.net.wujun.**.service.impl">
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>-->

    <!--<bean id="validator" class="org.springframework.validation.beanvalidation.LocalValidatorFactoryBean">
        <property name="providerClass" value="org.hibernate.validator.HibernateValidator"/>
    </bean>-->

</beans>